---
import "../styles/global.css";
import fs from "fs";
import matter from "gray-matter";
import { marked } from "marked";

const file = fs.readFileSync("./src/content/colores.md", "utf-8");
const { content } = matter(file);
const contenido_markdown = marked(content);
---

<html lang="es">
  <head>
    <meta charset="utf-8" />
    <title>Hola Cris ✨</title>
  </head>
  <body
    class="bg-white text-black flex flex-col items-center justify-center min-h-screen p-4"
  >
    <h1 class="text-6xl font-bold mb-8 drop-shadow-lg animate-bounce text-gray-900">
      Hola Cris 👋
    </h1>

    <!-- Imagen de Tomioka con animación -->
    <div class="relative w-64 h-64 mb-8">
      <img
        src="/tomioka.jpg"
        alt="Tomioka"
        class="w-full h-full object-cover rounded-xl shadow-2xl animate-bounce"
      />
    </div>

    <!-- Texto con colores desde Markdown -->
    <article class="prose text-center max-w-md" set:html={contenido_markdown}></article>
  </body>
</html>
